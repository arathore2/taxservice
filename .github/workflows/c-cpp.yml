name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - name: checkout-bitbucket
      run: git clone ${{https://bitbucket.org/energyconservatory/det/src/master/}}

